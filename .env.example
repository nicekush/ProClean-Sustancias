# Server-only: enter credentials through Netlify environment settings; never commit them.
FIREBASE_SERVICE_ACCOUNT_JSON=
# Explicit Firebase Authentication UIDs, separated by commas.
ADMIN_UIDS=
# Enable only after the company's operational acceptance.
OPERATIONS_ENABLED=false
